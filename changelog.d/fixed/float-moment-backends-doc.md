**docs/metrics/features.md** — corrected `float_moment` backend list from
"scalar only" to "CUDA, SYCL, Vulkan" (all three GPU implementations shipped
in T7-23; the per-extractor body text had not been updated to match the
overview table).
