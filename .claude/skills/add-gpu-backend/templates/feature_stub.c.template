/**
 *  Copyright 2026 Lusoris and Claude (Anthropic)
 *  SPDX-License-Identifier: BSD-3-Clause-Plus-Patent
 *
 *  Stub kernel for {{FEATURE}} on the {{BACKEND}} backend. Generated by
 *  /add-gpu-backend — replace with a real implementation.
 */

#include <errno.h>

#include "../common.h"

int vmaf_{{BACKEND}}_{{FEATURE}}_init(Vmaf{{BACKEND_PASCAL}}Context *ctx)
{
    (void) ctx;
    /* TODO: allocate device-side scratch buffers */
    return 0;
}

int vmaf_{{BACKEND}}_{{FEATURE}}_run(Vmaf{{BACKEND_PASCAL}}Context *ctx,
                                      const void *ref, const void *dis,
                                      int width, int height, int stride)
{
    (void) ctx; (void) ref; (void) dis;
    (void) width; (void) height; (void) stride;
    /* TODO: launch {{FEATURE}} kernel on {{BACKEND}} */
    return -ENOSYS;
}

void vmaf_{{BACKEND}}_{{FEATURE}}_destroy(Vmaf{{BACKEND_PASCAL}}Context *ctx)
{
    (void) ctx;
    /* TODO: free device-side scratch buffers */
}
