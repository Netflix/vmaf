/**
 *  Copyright 2026 Lusoris and Claude (Anthropic)
 *  SPDX-License-Identifier: BSD-3-Clause-Plus-Patent
 *
 *  {{FEATURE}} — {{ISA}} path. Generated by /add-simd-path — bit-exact
 *  against the scalar reference is a mandatory gate (see tests below).
 */

#include <stddef.h>
#include <stdint.h>

{{ISA_HEADER}}

#include "../{{FEATURE}}.h"

void vmaf_{{FEATURE}}_{{ISA}}(const uint8_t *ref, const uint8_t *dis,
                               int width, int height, int stride,
                               double *out)
{
    /*
     * TODO: vectorized {{FEATURE}} via {{ISA}} intrinsics.
     *
     * Checklist:
     *   - align load/store to {{ALIGN}} bytes; fall back to scalar tail.
     *   - reductions use double accumulation (see commit 24c88a32).
     *   - runtime dispatch hook is registered in ../{{FEATURE}}.c.
     *   - `bit_exact_vs_scalar_{{ISA}}` test in libvmaf/test guards this.
     */
    (void) ref; (void) dis; (void) width; (void) height; (void) stride;
    (void) out;
}
